#videos_wrapper

  - if @link

    .list_buttons
      = link_to "Auth", @link

  - else

    .list_buttons
      = link_to "Ajouter", new_admin_video_path, :remote => true

    %ul#videos.sortable
      = render :partial => @videos unless @videos.blank?


