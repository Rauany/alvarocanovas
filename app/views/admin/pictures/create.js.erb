$("#category_<%=@category.id%>").find('.content')
    .hide()
    .html('<%= render_j :partial => 'index', :locals => { :category => @category, :pictures => @category.pictures } %>')
    .fadeIn('slow');
