= labelled_form_for([:admin,@user], :remote => true) do |f|

  %h3= @user.new_record? ? "Créer un nouvel utilisateur" : "Modifier l'utilisateur"

  = f.error_messages :header_tag => "h4", :header_message => "", :header_message => nil

  = f.text_field :first_name
  = f.text_field :last_name
  = f.text_field :email
  = f.text_field :phone_number
  = f.text_field :address
  = f.text_field :zip_code
  = f.text_field :city  
  = f.text_field :country

  .buttons
    = link_to "Annuler", @user.new_record? ? admin_users_path : admin_user_path(@user), :class => "cancel", :remote => true
    = f.submit "OK"


